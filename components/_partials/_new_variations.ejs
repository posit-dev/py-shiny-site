## Variations

<% for (const item of items) { %>

#### <%= item.title %> {.mb-0 .mt-5}

::: {.variation-description}
<%= item.description %>
:::

<%= include('_new_shiny-example-panel.ejs', item) %>

<% } %>
